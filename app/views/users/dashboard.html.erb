<div class="container">
  <% @current_user.received_tymls.order('created_at desc').each do |tyml| %>

    <% unless tyml.archived == true %>

    <% @tyml_comment = tyml %>

    <%= render 'tymls/tyml', :tyml => tyml %>

    <% end %>
  <% end %>
</div>
